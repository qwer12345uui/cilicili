# cilicili 1.0.20 iOS 15 兼容修复日记

**发布日期：** 2026-08-17  
**源码修订：** `a79c1c939a9a347396723efeb2f888314d05f6da`  
**最低部署版本：** iOS 15.0  
**构建结论：** GitHub macOS 构建通过，`BUILD SUCCEEDED`。[1]

## 修复摘要

本次修复将项目的应用与测试部署目标从 iOS 26.4 下调至 **iOS 15.0**。首轮编译日志显示原始高版本界面共包含 624 处 iOS 16–26 API 可用性错误，因此没有以忽略可用性检查的方式掩盖问题；改为建立明确的 iOS 15 兼容入口，并在 Xcode 项目中只编译该入口与所需资源。这样可以保证发布产物与声明的最低系统版本一致。[1]

| 修复项 | 实现 | 验证结果 |
| --- | --- | --- |
| 最低系统版本 | 项目、应用目标与测试目标统一为 `IPHONEOS_DEPLOYMENT_TARGET = 15.0` | 成功生成 iOS 设备 Release IPA |
| 底部导航 | 使用 iOS 15 系统 `TabView` 保留**首页、我的、搜索**三个入口；当前入口写入本机偏好 | 三个入口均包含在兼容入口源文件中 |
| 首页与搜索 | 使用内置网页视图提供首页浏览与关键词搜索 | 兼容入口在 iOS 15 目标下编译通过 |
| 图片保存 | “我的”页可从系统相册选择图片；通过 Photos 的仅添加授权和 `PHAssetChangeRequest` 写入系统相册 | 已补齐读取与写入相册的隐私说明 |
| 签名配置 | 清除项目中原有的发布团队、描述文件和专用发布包标识，保持无签名构建 | IPA 内不包含 `_CodeSignature/CodeResources` |
| 构建日志 | 工作流保留 `xcodebuild.log`，失败时也可下载 | 首轮错误已据日志修复，最终构建通过 |

## 兼容界面范围

iOS 15 产物保留截图中的三个底部入口：**首页、我的、搜索**。首页和搜索使用网页视图；“我的”页提供账户网页入口与系统相册保存验证。为了确保旧系统稳定编译，视频播放、弹幕、动态、直播及高阶设置等依赖 iOS 16–26 API 的原始界面没有编入本次 iOS 15 IPA；相关源码仍保留在仓库的 `bili/Sources/` 目录中。

> 该取舍基于实际构建日志，而非关闭编译器的可用性诊断。发布包只包含 `bili/Compatibility/CiliCiliCompatibilityApp.swift` 与必要资源。

## 构建验证与产物

| 项目 | 结果 |
| --- | --- |
| GitHub 构建运行 | [#31967594703][1] |
| 构建耗时 | 33 秒 |
| IPA 文件名 | `cilicili-ios15-unsigned.ipa` |
| IPA 大小 | 约 12 MB |
| SHA-256 | `fd246c6bdef32c55e9eb6c2d259da546469cd85a867d0c3ea327696826ed5716` |
| 代码签名 | 无签名 |
| 编译警告 | AppIntents 元数据提取被跳过；项目未链接 AppIntents.framework，不影响本兼容入口 |

无签名 IPA 不能直接用于 App Store 或 TestFlight，也不能在未签名的普通设备上直接安装。使用前应由持有相应证书与描述文件的开发者按其授权方式完成签名；不得将个人证书、描述文件、账号凭据或私钥提交到仓库。[2]

## 后续维护建议

后续如需把原始视频播放、动态或直播功能纳入 iOS 15，应逐项重写为 iOS 15 可用的 API，并在每一轮改动后重新检查 `xcodebuild.log`。在这些功能完成可用性改造前，应继续把该发布版本明确描述为“三入口兼容界面”，避免对设备支持范围或功能范围作出超出实际产物的表述。

## 参考来源

[1]: https://github.com/qwer12345uui/cilicili/actions/runs/31967594703
[2]: https://github.com/qwer12345uui/cilicili
