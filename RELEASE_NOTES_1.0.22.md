# cilicili 1.0.22 原生媒体功能修复日记

**最低部署版本：** iOS 15.0

**应用版本：** 1.0.22（259）

**源码修订：** `c681414d376bdb70a6b14dd30feddbcc4e4e0e52`

**构建结论：** GitHub macOS 构建通过，构建日志显示 `BUILD SUCCEEDED`。[1]

## 本次实现

本版本在原生 SwiftUI 三入口框架上补齐了可运行的视频内容、搜索、账号和媒体保存链路。首页不再展示固定演示卡片，而是请求公开推荐接口并将真实视频封面、标题、作者和时长展示为原生内容卡片。搜索页面接入可用的聚合搜索入口，按视频分组显示真实搜索结果；点击首页或搜索结果都可进入原生播放器页面。[2]

| 能力 | 原生实现 | 行为范围 |
| --- | --- | --- |
| 视频浏览 | 推荐接口、网络封面、原生内容卡片 | 展示公开可获取的视频元数据；网络、区域与平台策略可能影响返回结果。 |
| 视频播放 | `AVPlayerViewController`、系统播放控制、画中画能力 | 从视频详情取得播放地址后交由系统播放器播放；付费、受限、版权或登录要求内容会由上游服务决定是否返回可用地址。 |
| 视频画面保存 | `AVAssetImageGenerator` 读取当前播放时间的图像，再通过 Photos 写入系统相册 | 保存的是当前帧静态图片，不下载、转存或绕过受限视频的原始媒体文件。 |
| 原生搜索 | 聚合搜索接口、关键词输入、热门词与结果列表 | 返回视频分组中的可播放候选项。 |
| 账号登录 | 二维码生成、扫码状态轮询、登录状态刷新 | 采用账号客户端扫码确认流程；不提供密码输入，也不在源代码或发布包中写入账号信息。 |
| 凭据保护 | Keychain 通用密码项，`AfterFirstUnlockThisDeviceOnly` 可访问策略 | 仅保存本机完成扫码后得到的会话 Cookie；用户可在“我的”页退出并清除本地会话。 |

## 编译错误与修复

首轮构建在会话 Cookie 字符串拼接处报告字符串插值未闭合。已按错误行修正插值表达式并重新构建。第二轮构建提示 `ObservableObject` 和 `@Published` 缺少其定义模块；已显式导入 `Combine`。第三轮构建通过。[1]

| 构建运行 | 结论 | 处理 |
| --- | --- | --- |
| [#31971935192][3] | 失败，字符串插值语法错误 | 修正 `persistCookiesFromSharedStorage` 的 Cookie 拼接。 |
| [#31972009989][4] | 失败，缺少 `Combine` | 导入 `Combine`，恢复可观察数据与状态存储。 |
| [#31972072829][1] | 通过 | 生成 iOS 15 无签名 IPA。 |

## 构建验证与产物

| 项目 | 结果 |
| --- | --- |
| GitHub 构建运行 | [#31972072829][1] |
| 构建时长 | 35 秒 |
| IPA 文件名 | `cilicili-ios15-unsigned.ipa` |
| IPA 大小 | 11,770,140 bytes |
| SHA-256 | `1f941147cd16c4f340c0e1317ba4be1b6fc3051caf2d6c5d2eaec9b911c68d61` |
| 签名状态 | 无签名 |

> 无签名 IPA 不能直接作为 App Store 或 TestFlight 的发布包，也不能直接安装到未签名的普通设备。应由具备相应授权的开发者在仓库外进行签名；不得将个人证书、描述文件、账号信息、Cookie 或私钥提交到仓库。

## 功能边界

本版本落实视频浏览、搜索、二维码账号状态、原生播放和当前画面保存的主链路。原始高版本工程中的弹幕渲染、直播间、收藏/投币/评论写入、多账号实验、下载原始媒体文件及其他依赖 iOS 16–26 API 的页面，仍未纳入 iOS 15 兼容目标。对受版权、付费、区域或账号权限限制的内容，应用只使用服务方返回的可用播放地址，不规避访问控制或数字版权保护。

## 参考来源

[1]: https://github.com/qwer12345uui/cilicili/actions/runs/31972072829
[2]: https://github.com/qwer12345uui/cilicili
[3]: https://github.com/qwer12345uui/cilicili/actions/runs/31971935192
[4]: https://github.com/qwer12345uui/cilicili/actions/runs/31972009989
