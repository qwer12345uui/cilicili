# cilicili 1.0.23 封面与播放可靠性修复日记

**最低部署版本：** iOS 15.0

**应用版本：** 1.0.23（260）

**源码修订：** `4f6835da8dd1c4016c56e56c0fa17f0b57498639`

**构建结论：** GitHub macOS 构建通过，生成无签名 IPA。[1]

## 录屏问题与修复

用户录屏显示首页与搜索结果的所有封面均为紫色渐变占位图，点击卡片后播放器保持黑色或播放图标，未呈现可用媒体。经核验，公开推荐接口会返回 `http://` 和协议相对形式的 CDN 封面链接；在 iOS 15 的传输安全规则下，HTTP 图片请求不能作为正常封面加载路径。本版本将所有封面地址统一规范为 HTTPS，并将卡片封面改为真实图片、居中播放图标、时长角标和明确的加载失败状态。

| 项目 | 修复前 | 1.0.23 修复后 |
| --- | --- | --- |
| 视频封面 | HTTP 地址进入 `AsyncImage`，界面显示紫色占位图 | 统一升级至 HTTPS；可用封面显示真实图片，失败时显示明确的图片加载提示。 |
| 视频卡片 | 占位视觉掩盖真实加载状态 | 按双列内容卡片布局呈现封面、播放图标、时长、标题和作者。 |
| 内嵌播放器 | 设置 URL 后立即视为可播放，黑屏时没有可靠状态反馈 | 监听 `AVPlayerItem` 就绪与失败状态，只有流真正可用后才显示内嵌播放器与画面保存操作。 |
| 受限播放流 | 流被服务端拒绝时容易停留在黑色播放器 | 显示具体失败状态，并提供“使用官方播放器打开”：优先唤起哔哩哔哩，未安装时打开官方视频网页。 |
| 视频画面保存 | 黑屏或未就绪时仍可尝试保存 | 仅在内嵌流已就绪时允许抓取当前帧并写入系统相册。 |

## 登录入口

保留二维码登录，同时增加“手机号验证码登录”和“账号密码登录”入口。两个入口均转至官方登录页面，手机号、验证码与密码只在官方页面中处理；应用界面、源代码、运行日志和仓库不会读取或保存这些敏感凭据。完成二维码授权后的会话仍按系统钥匙串策略仅存储在当前设备。

> 内嵌播放是否可用仍取决于服务端返回的可访问媒体流、账号权限、地区、版权与网络条件。对于受限内容，应用不会规避访问控制；应使用官方播放器或官方网页播放。

## 构建验证与产物

| 项目 | 结果 |
| --- | --- |
| GitHub 构建运行 | [#31983565529][1] |
| 构建时长 | 1 分 6 秒 |
| IPA 文件名 | `cilicili-ios15-unsigned.ipa` |
| IPA 大小 | 11,788,677 bytes |
| SHA-256 | `642f31ffe48d26305084ece8f970ea090d2d58b035ce49a02190880721769e86` |
| 签名状态 | 无签名 |

旧发布版本 `v1.0.20-ios15`、`v1.0.21-ios15` 和 `v1.0.22-ios15` 中的 IPA 与校验和附件已删除；其源代码、发布日记和构建记录仍保留在仓库。当前版本发布后，只有 `v1.0.23-ios15` 提供 IPA 附件。

> 无签名 IPA 不能直接作为 App Store、TestFlight 或普通设备安装包。请由具备相应授权的开发者在仓库外进行签名，且不要将个人证书、描述文件、账号信息、Cookie 或私钥提交到仓库。

## 参考来源

[1]: https://github.com/qwer12345uui/cilicili/actions/runs/31983565529
