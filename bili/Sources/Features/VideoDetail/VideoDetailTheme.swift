import SwiftUI

enum VideoDetailTheme {
    static let background = Color(.systemGroupedBackground)

    static let surface = Color(.secondarySystemGroupedBackground)

    static let secondarySurface = Color(.tertiarySystemGroupedBackground)
}
