# cilicili 1.0.21 原生界面修复日记

**最低部署版本：** iOS 15.0  
**应用版本：** 1.0.21（258）  
**状态：** 待完成 GitHub 构建验证。

## 本次修复

上一版兼容界面使用网页容器承载首页与搜索，外观和交互更接近网页。本次修复将 iOS 15 兼容入口改为完全由 SwiftUI、UIKit 与 Photos 组成的原生框架，不再包含 `WKWebView` 或网页容器。首页、我的、搜索三个入口均在 `bili/Compatibility/CiliCiliCompatibilityApp.swift` 中实现，并继续以 iOS 15 为最低部署版本。

| 修复项 | 实现内容 |
| --- | --- |
| 底部框架 | 使用自定义悬浮胶囊导航替代系统标签栏；采用半透明材质、圆角轮廓、阴影及粉色选中态，对应截图中的视觉层级。 |
| 首页 | 提供原生标题、推荐轮播、热门内容卡片与时长标识，不再加载外部网页。 |
| 搜索 | 提供原生关键词输入、热门词、推荐功能卡片和本地结果列表。 |
| 我的 | 提供原生账户概览、本地功能入口和偏好项。 |
| 图片保存 | 延续系统相册选择、仅添加授权、Photos 写入和失败提示。 |
| 状态恢复 | 保存并恢复上一次选中的首页、我的或搜索入口。 |

## 功能范围

本版本实现的是 iOS 15 原生应用框架和可交互内容骨架。原始工程中依赖 iOS 16–26 API 的视频播放、弹幕、动态、直播及高阶设置并未编入该 IPA，相关源代码保留在 `bili/Sources/` 中，未作为本次兼容构建的一部分。

> 此版本不以网页视图替代应用主页面。除系统相册选择器外，首页、我的、搜索均使用原生视图与原生状态管理实现。

## 构建验证

构建运行编号、IPA 文件大小、SHA-256 和日志结论将在 GitHub 构建成功后补充到本日记中。

## 参考来源

[1]: https://github.com/qwer12345uui/cilicili
